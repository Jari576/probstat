clc
clearvars;

load('machine.mat')

meme = mean(performance);
medaian = median(performance);
mehd = mad(performance);

boxplot(performance);

for k=1:6
  hist = hist(measurements(:,k))
  saveas(hist,sprintf('hist_%d.jpg',k))
end